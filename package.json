{
  "name": "SortMyFiles",
  "displayName": "SortMyFiles",
  "description": "Re-order files and folder as you wish in VSCode",
  "version": "0.1.7",
  "engines": {
    "vscode": "^1.96.0"
  },
  "publisher": "CanklotSoftware",
  "icon": "images/icon128.png",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onDidSaveTextDocument"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "SortMyFiles",
      "properties": {
        "sortmyfiles.configMode": {
          "type": "string",
          "default": ".order",
          "enum": [".order", "Hugo"],
          "description": "Determines whether to use '.order' file or Hugo weight system for sorting"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/canklot/SortMyFiles"
  },
  "dependencies": {
    "minimatch": "^10.0.1"
  }
}